<h1> Home#index </h1>
<% if user_signed_in? %>
   Tem sessão iniciada como <%= current_user.email %>, não devia?
   <%= link_to "Terminar sessão.", destroy_user_session_path, :method => :delete %>
<% else %>
   Por favor ou   
   <%= link_to "registe-se", new_user_registration_path %> ou <%= link_to "Inicie sessão", new_user_session_path %>
<% end %>
